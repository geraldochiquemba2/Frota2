name: Render Keep-Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Service
        run: |
          curl -s -f https://frota2.onrender.com/api/healthz || echo "Ping failed"
